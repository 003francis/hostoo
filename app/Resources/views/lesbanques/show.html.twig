{% extends 'base.html.twig' %}

{% block body %}
    <h1>LesBanques</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lesBanque.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ lesBanque.nom }}</td>
            </tr>
            <tr>
                <th>Compte</th>
                <td>{{ lesBanque.compte }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('lesbanques_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('lesbanques_edit', { 'id': lesBanque.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
