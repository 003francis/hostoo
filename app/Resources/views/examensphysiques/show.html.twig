
    <h1>ExamensPhysiques</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ examensPhysique.id }}</td>
            </tr>
            <tr>
                <th>Observation</th>
                <td>{{ examensPhysique.observation }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('examensphysiques_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('examensphysiques_edit', { 'id': examensPhysique.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>

