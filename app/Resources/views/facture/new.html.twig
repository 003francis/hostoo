{% extends 'base.html.twig' %}

{% block body %}
    <h1>Facture creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Enregistrer" class="btn btn-success" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('edition-facture_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
