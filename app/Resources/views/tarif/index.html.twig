{% extends 'base.html.twig' %}

{% block body %}
    <h1>Tarif list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Prix</th>
                <th>Dateprix</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tarif in tarifs %}
            <tr>
                <td><a href="{{ path('facture_tarif_show', { 'id': tarif.id }) }}">{{ tarif.id }}</a></td>
                <td>{{ tarif.prix }}</td>
                <td>{% if tarif.dateprix %}{{ tarif.dateprix|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('facture_tarif_show', { 'id': tarif.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('facture_tarif_edit', { 'id': tarif.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('facture_tarif_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
